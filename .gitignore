target/
.env
*.log
sbom.cdx.json

# IDE
.idea/
.vscode/
*.iml
.run/

# OS
.DS_Store
Thumbs.db

# JVM crash dumps
hs_err_pid*
replay_pid*

# Runtime backup artifacts - produced on the host by the redis-backup sidecar
# (monitoring/backup-entrypoint.sh). Content is re-creatable; see RUNBOOK.
backups/

# Local infra state (Caddy/Redis data dirs, if any)
caddy/data/
caddy/config/

# NOTE: keystore.p12 stays committed ON PURPOSE - it is a self-signed dev-only
# certificate for the dev profile. Regenerate it and keep the real password in
# your secret store (see README "TLS").
